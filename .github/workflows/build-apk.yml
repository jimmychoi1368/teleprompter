# =============================================================================
# GitHub Actions workflow for building Android APK with Buildozer
# =============================================================================
# 【重要声明】本配置文件仅属于「teleprompter」仓库
# 
# 【v4.0 - 调试优先版】
# 核心目标：输出详细日志，定位Build APK步骤失败的真正原因
# =============================================================================
name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_NDK_VERSION: 25.2.9519653
      ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/25.2.9519653

    steps:
      # =========================================
      # 步骤1：拉取代码
      # =========================================
      - name: Checkout code
        uses: actions/checkout@v4

      # =========================================
      # 步骤2：显示项目结构（调试）
      # 目的：确认代码和文件是否正确拉取
      # =========================================
      - name: Show Project Structure
        run: |
          echo "📁 Project structure:"
          ls -la
          echo ""
          echo "📄 buildozer.spec content:"
          cat buildozer.spec
          echo ""
          echo "📄 main.py exists:"
          ls -la main.py || echo "main.py NOT FOUND!"

      # =========================================
      # 步骤3：设置JDK 17
      # =========================================
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # =========================================
      # 步骤4：设置Python环境
      # =========================================
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # =========================================
      # 步骤5：设置Android SDK
      # =========================================
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          accept-android-sdk-licenses: true

      # =========================================
      # 步骤6：全面接受SDK许可
      # =========================================
      - name: Accept All SDK Licenses
        run: |
          echo "📜 Accepting all SDK licenses..."
          yes | sdkmanager --licenses || true
          
          mkdir -p $ANDROID_SDK_ROOT/licenses
          echo -e "\n8933bad161af4178b1185d1a37fbf41ea5269c55" > $ANDROID_SDK_ROOT/licenses/android-sdk-license
          echo -e "\nd56f5187479451eabf01fb78af6dfcb131a6481e" >> $ANDROID_SDK_ROOT/licenses/android-sdk-license
          echo -e "\n24333f8a63b6825ea9c5514f83c2829b004d1fee" >> $ANDROID_SDK_ROOT/licenses/android-sdk-license
          echo -e "\n84831b9409646a918e30573bab4c9c91346d8abd" > $ANDROID_SDK_ROOT/licenses/android-sdk-preview-license
          
          echo "✅ Licenses accepted!"

      # =========================================
      # 步骤7：安装SDK组件
      # =========================================
      - name: Install SDK Components
        run: |
          echo "📦 Installing SDK components..."
          sdkmanager "platform-tools"
          sdkmanager "platforms;android-33"
          sdkmanager "build-tools;33.0.2"
          sdkmanager "build-tools;34.0.0"
          sdkmanager "ndk;$ANDROID_NDK_VERSION"
          
          echo "✅ SDK components installed!"
          echo ""
          echo "📋 Installed components:"
          sdkmanager --list_installed

      # =========================================
      # 步骤8：配置环境变量和PATH
      # =========================================
      - name: Configure Environment
        run: |
          echo "🔧 Configuring environment..."
          
          # 添加到PATH
          echo "$ANDROID_SDK_ROOT/build-tools/34.0.0" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/build-tools/33.0.2" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/platform-tools" >> $GITHUB_PATH
          
          # 创建符号链接
          sudo ln -sf $ANDROID_SDK_ROOT/build-tools/34.0.0/aapt /usr/local/bin/aapt || true
          sudo ln -sf $ANDROID_SDK_ROOT/build-tools/34.0.0/aapt2 /usr/local/bin/aapt2 || true
          
          echo "✅ Environment configured!"

      # =========================================
      # 步骤9：安装系统依赖
      # 【关键】确保所有Kivy编译依赖都安装
      # =========================================
      - name: Install System Dependencies
        run: |
          echo "📦 Installing system dependencies..."
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            git \
            zip \
            unzip \
            python3-pip \
            python3-dev \
            ffmpeg \
            libsdl2-dev \
            libsdl2-image-dev \
            libsdl2-mixer-dev \
            libsdl2-ttf-dev \
            libportmidi-dev \
            libswscale-dev \
            libavformat-dev \
            libavcodec-dev \
            libavdevice-dev \
            zlib1g-dev \
            libgstreamer1.0-dev \
            gstreamer1.0-plugins-base \
            gstreamer1.0-plugins-good \
            libgstreamer-plugins-base1.0-dev \
            libffi-dev \
            libssl-dev \
            autoconf \
            automake \
            libtool \
            pkg-config \
            wget \
            lld \
            ccache \
            cmake \
            ninja-build \
            libltdl-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo5 \
            patch \
            ant
          
          echo "✅ System dependencies installed!"

      # =========================================
      # 步骤10：安装Python依赖
      # =========================================
      - name: Install Python Dependencies
        run: |
          echo "📦 Installing Python dependencies..."
          python -m pip install --upgrade pip wheel setuptools
          pip install buildozer==1.5.0
          pip install cython==0.29.33
          pip install kivy==2.2.1
          pip install virtualenv
          
          echo ""
          echo "📋 Installed Python packages:"
          pip list | grep -E "buildozer|cython|kivy"

      # =========================================
      # 步骤11：下载Vosk模型
      # 【关键】确保模型在正确位置
      # =========================================
      - name: Download and Setup Vosk Model
        run: |
          echo "📥 Downloading Vosk model..."
          
          # 下载模型
          wget -q https://alphacephei.com/vosk/models/vosk-model-small-en-us-0.15.zip
          unzip -q vosk-model-small-en-us-0.15.zip
          
          # 验证模型存在
          echo ""
          echo "📁 Vosk model structure:"
          ls -la vosk-model-small-en-us-0.15/ || echo "Model directory NOT FOUND!"
          
          echo "✅ Vosk model ready!"

      # =========================================
      # 步骤12：彻底清理Buildozer缓存
      # =========================================
      - name: Clean Buildozer Cache
        run: |
          echo "🧹 Cleaning all Buildozer caches..."
          rm -rf ~/.buildozer 2>/dev/null || true
          rm -rf ~/.cache/buildozer 2>/dev/null || true
          rm -rf .buildozer 2>/dev/null || true
          echo "✅ Caches cleaned!"

      # =========================================
      # 步骤13：验证构建环境
      # =========================================
      - name: Verify Build Environment
        run: |
          echo "🔍 Verifying build environment..."
          echo ""
          echo "=== Java Version ==="
          java -version
          echo ""
          echo "=== Python Version ==="
          python --version
          echo ""
          echo "=== Buildozer Version ==="
          buildozer --version
          echo ""
          echo "=== Android SDK ==="
          echo "ANDROID_SDK_ROOT: $ANDROID_SDK_ROOT"
          echo "ANDROID_HOME: $ANDROID_HOME"
          ls -la $ANDROID_SDK_ROOT/build-tools/
          echo ""
          echo "=== aapt Check ==="
          which aapt && aapt version || echo "aapt not found in PATH"
          ls -la $ANDROID_SDK_ROOT/build-tools/34.0.0/aapt || echo "aapt not in 34.0.0"
          echo ""
          echo "=== Project Files ==="
          ls -la
          echo ""
          echo "✅ Environment verification complete!"

      # =========================================
      # 步骤14：【核心步骤】构建APK
      # 【关键修复①】输出详细日志到文件
      # =========================================
      - name: Build APK with Buildozer (Verbose)
        id: build
        run: |
          echo "🔨 Starting Buildozer build..."
          echo "================================================"
          echo "Build started at: $(date)"
          echo "================================================"
          
          # 设置环境变量
          export ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT
          export ANDROID_HOME=$ANDROID_HOME
          export ANDROID_NDK_HOME=$ANDROID_NDK_HOME
          export PATH="$ANDROID_SDK_ROOT/build-tools/34.0.0:$ANDROID_SDK_ROOT/build-tools/33.0.2:$ANDROID_SDK_ROOT/platform-tools:/usr/local/bin:$PATH"
          
          # 执行构建，输出到日志文件并同时显示
          # 使用 set +e 允许命令失败后继续执行
          set +e
          
          buildozer -v android debug 2>&1 | tee buildozer_full.log
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          
          set -e
          
          echo ""
          echo "================================================"
          echo "Build finished at: $(date)"
          echo "Build exit code: $BUILD_EXIT_CODE"
          echo "================================================"
          
          # 保存退出码供后续步骤使用
          echo "build_exit_code=$BUILD_EXIT_CODE" >> $GITHUB_OUTPUT
          
          # 检查APK是否生成
          if ls bin/*.apk 1>/dev/null 2>&1; then
            echo "✅ APK file(s) found!"
            ls -lh bin/*.apk
            echo "apk_exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ No APK files found in bin/"
            echo "apk_exists=false" >> $GITHUB_OUTPUT
          fi
        env:
          BUILDOZER_WARN_ON_ROOT: 0

      # =========================================
      # 步骤15：【调试】显示构建日志最后500行
      # 无论成功失败都显示
      # =========================================
      - name: Show Build Log (Last 500 lines)
        if: always()
        run: |
          echo "📋 Build log (last 500 lines):"
          echo "================================================"
          tail -500 buildozer_full.log 2>/dev/null || echo "No build log found"

      # =========================================
      # 步骤16：【调试】显示错误关键词
      # =========================================
      - name: Search for Errors in Log
        if: always()
        run: |
          echo "🔍 Searching for errors in build log..."
          echo ""
          echo "=== Lines containing 'error' ==="
          grep -i "error" buildozer_full.log 2>/dev/null | tail -50 || echo "No 'error' found"
          echo ""
          echo "=== Lines containing 'failed' ==="
          grep -i "failed" buildozer_full.log 2>/dev/null | tail -30 || echo "No 'failed' found"
          echo ""
          echo "=== Lines containing 'exception' ==="
          grep -i "exception" buildozer_full.log 2>/dev/null | tail -20 || echo "No 'exception' found"
          echo ""
          echo "=== Lines containing 'not found' ==="
          grep -i "not found" buildozer_full.log 2>/dev/null | tail -20 || echo "No 'not found' found"
          echo ""
          echo "=== Lines containing 'license' ==="
          grep -i "license" buildozer_full.log 2>/dev/null | tail -10 || echo "No 'license' found"

      # =========================================
      # 步骤17：上传构建日志（无论成功失败）
      # =========================================
      - name: Upload Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: buildozer-log
          path: buildozer_full.log
          retention-days: 7
          if-no-files-found: ignore

      # =========================================
      # 步骤18：上传APK（仅当APK存在时）
      # 【关键修复②】添加条件判断
      # =========================================
      - name: Upload APK artifact
        if: steps.build.outputs.apk_exists == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: teleprompter-apk
          path: bin/*.apk
          retention-days: 30

      # =========================================
      # 步骤19：构建结果摘要
      # =========================================
      - name: Build Summary
        if: always()
        run: |
          echo "## 📱 Build Result" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.build.outputs.apk_exists }}" == "true" ]; then
            echo "### ✅ APK Generated Successfully!" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            ls -lh bin/*.apk >> $GITHUB_STEP_SUMMARY 2>/dev/null
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ APK Generation Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Build exit code: ${{ steps.build.outputs.build_exit_code }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Check the uploaded build log for details.**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 🔍 Quick Error Summary" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            grep -i "error\|failed\|exception" buildozer_full.log 2>/dev/null | tail -20 >> $GITHUB_STEP_SUMMARY || echo "See full log" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

      # =========================================
      # 步骤20：如果构建失败则报错退出
      # =========================================
      - name: Fail if APK not generated
        if: steps.build.outputs.apk_exists != 'true'
        run: |
          echo "❌ Build failed - APK was not generated"
          echo "Please check the build log artifact for details"
          exit 1
